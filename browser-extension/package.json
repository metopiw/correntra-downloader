{
  "name": "correntra-browser-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome and Edge integration for Correntra Downloader",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run typecheck && node --import tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "0.2.5",
    "@types/node": "26.2.0",
    "esbuild": "0.28.2",
    "tsx": "4.23.12",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=22"
  }
}
